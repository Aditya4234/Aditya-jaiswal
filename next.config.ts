import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
  turbopack: {
    root: "/home/aditya-gupta/new project is the complete full stack /portfolio/frontend",
  },
};

export default nextConfig;
